<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:textColor">@color/primaryText</item>
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="Title" parent="TextAppearance.AppCompat.Title">
        <item name="android:textColor">@color/primaryText</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="Body" parent="TextAppearance.AppCompat.Body1">
        <item name="android:textColor">@color/primaryText</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="Body2" parent="TextAppearance.AppCompat.Body2">
        <item name="android:textColor">@color/primaryText</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="PreferenceScreen">
        <item name="android:textColor">@color/primaryText</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="SubTitle" parent="TextAppearance.AppCompat.Widget.ActionBar.Subtitle">
        <item name="android:textColor">@color/primaryText</item>
        <item name="android:textSize">@dimen/text_l</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="ShapeableImageViewCover">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">@dimen/rounded_corner</item>
    </style>

</resources>
